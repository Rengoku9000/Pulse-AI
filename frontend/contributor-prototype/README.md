# Contributor Prototype

This folder preserves the standalone prototype that was pushed by another contributor on the remote branch:

```text
backup-remote-e63564d
```

It is intentionally kept separate from the deployable PulseGuard AI app.

Current deployable frontend:

```text
frontend/src/main.jsx
frontend/src/styles.css
```

This prototype uses browser CDN scripts and global `window.React` patterns, so it is not wired into the Vite/Kubernetes production path yet. Use it as a design/reference source when merging additional cinematic interactions into the main PulseGuard AI command center.

